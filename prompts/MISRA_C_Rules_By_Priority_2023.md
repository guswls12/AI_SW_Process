 # MISRA C 규칙 분석 자료 (중요도별 정리) - 2023

> **출처**: 제공된 MISRA 규칙 설정 파일 (M3CM 기반, MISRA C:2023 추정)
> **정리 기준**: 준수 의무 수준(Mandatory → Required → Advisory) → 주제별 그룹핑
> **총 규칙 수**: 약 183개

---

# 🔴 1부. Mandatory (필수 규칙) — 약 23개

> **절대 위반해서는 안 되는 규칙.** 

## 🔸 1-1. 초기화·메모리 접근

| 번호 | 한국어 설명 |
|------|-------------|
| Rule-9.1 | 자동 저장 기간 객체는 값을 설정하기 전에 읽지 말 것 |
| Rule-9.7 | 원자(atomic) 객체는 접근 전에 적절히 초기화할 것 |
| Rule-19.1 | 중첩(overlapping) 객체에 대입하거나 복사하지 말 것 |

## 🔸 1-2. 함수 관련  

| 번호 | 한국어 설명 |
|------|-------------|
| Rule-17.3 | 함수를 암시적으로 선언하지 말 것 |
| Rule-17.4 | void 아닌 반환 타입 함수의 모든 출구 경로는 표현식을 가진 return 문이어야 할 것 |
| Rule-17.6 | 배열 파라미터 선언의 `[ ]` 안에 static 키워드를 포함하지 말 것 |
| Rule-17.9 | `_Noreturn` 지정자가 붙은 함수는 호출자로 반환되지 말 것 |

## 🔸 1-3. 표현식·sizeof

| 번호 | 한국어 설명 |
|------|-------------|
| Rule-12.5 | sizeof 피연산자는 `array of type`으로 선언된 함수 파라미터여서는 안 됨 |
| Rule-13.6 | sizeof 연산자의 피연산자는 잠재적 부수 효과를 가지는 표현식을 포함하지 말 것 |

## 🔸 1-4. 리터럴·포인터

| 번호 | 한국어 설명 |
|------|-------------|
| Rule-7.5 | 정수 상수 매크로의 인자는 적절한 형식을 가져야 할 것 |
| Rule-18.10 | 가변 수정(variably-modified) 배열 타입 포인터를 사용하지 말 것 |

## 🔸 1-5. 표준 라이브러리 (문자열·ctype·locale)

| 번호 | 한국어 설명 |
|------|-------------|
| Rule-21.13 | `<ctype.h>` 함수의 인자는 unsigned char로 표현 가능하거나 EOF여야 할 것 |
| Rule-21.17 | `<string.h>` 문자열 함수 사용 시 객체 경계를 넘는 접근을 하지 말 것 |
| Rule-21.18 | `<string.h>` 함수에 전달되는 size_t 인자는 적절한 값을 가져야 할 것 |
| Rule-21.19 | `localeconv`, `getenv`, `setlocale`, `strerror` 반환 포인터는 const로만 사용할 것 |
| Rule-21.20 | `asctime`, `ctime`, `gmtime` 등의 반환 포인터는 동일 함수 재호출 후 사용하지 말 것 |
| Rule-21.22 | `<tgmath.h>` 타입 제네릭 매크로의 모든 피연산자는 적절한 필수 타입이어야 할 것 |

## 🔸 1-6. 파일·메모리 자원 관리

| 번호 | 한국어 설명 |
|------|-------------|
| Rule-22.2 | 표준 라이브러리로 할당된 메모리 블록만 해제할 것 |
| Rule-22.4 | 읽기 전용으로 열린 스트림에 쓰기를 시도하지 말 것 |
| Rule-22.5 | FILE 객체 포인터를 역참조하지 말 것 |
| Rule-22.6 | 스트림이 닫힌 후에는 해당 FILE 포인터 값을 사용하지 말 것 |

## 🔸 1-7. 멀티스레드 안전

| 번호 | 한국어 설명 |
|------|-------------|
| Rule-22.12 | 스레드 객체·동기화 객체·스레드 고유 저장 포인터는 해당 표준 라이브러리 함수로만 접근할 것 |
| Rule-22.14 | 스레드 동기화 객체는 접근 전에 초기화할 것 |
| Rule-22.20 | 스레드 고유 저장 포인터는 접근 전에 생성되어야 할 것 |

---

# 🟠 2부. Required (요구 규칙) — 약 121개

## 🔹 2-1. 언어 환경·코드 품질

| 번호 | 한국어 설명 |
|------|-------------|
| Rule-1.1 | 표준 C 문법·제약 위반 금지, 구현체 변환 한계 초과 금지 |
| Rule-1.3 | 정의되지 않은 동작/중대한 미지정 동작 금지 |
| Rule-1.4 | 신규 등장(emergent) 언어 기능 사용 금지 |
| Rule-1.5 | 폐기 예정(obsolescent) 언어 기능 사용 금지 |
| Rule-2.1 | 도달 불가능한 코드(unreachable code) 금지 |
| Rule-2.2 | 죽은 코드(dead code) 금지 |

## 🔹 2-2. 주석·어휘

| 번호 | 한국어 설명 |
|------|-------------|
| Rule-3.1 | 주석 내에 `/*`나 `//` 문자 시퀀스 사용 금지 |
| Rule-3.2 | `//` 주석 내 줄 이어쓰기(line-splicing) 금지 |
| Rule-4.1 | 8진수·16진수 이스케이프 시퀀스는 종결되어야 함 |

## 🔹 2-3. 식별자 (Identifiers)

| 번호 | 한국어 설명 |
|------|-------------|
| Rule-5.1 | 외부 식별자는 서로 구별되어야 함 |
| Rule-5.2 | 동일 스코프·네임스페이스 내 식별자는 구별되어야 함 |
| Rule-5.3 | 내부 스코프 식별자가 외부 스코프 식별자를 가려서는 안 됨 |
| Rule-5.4 | 매크로 식별자는 구별되어야 함 |
| Rule-5.5 | 식별자는 매크로 이름과 구별되어야 함 |
| Rule-5.6 | typedef 이름은 고유해야 함 |
| Rule-5.7 | 태그(tag) 이름은 고유해야 함 |
| Rule-5.8 | 외부 링키지 객체·함수 식별자는 고유해야 함 |

## 🔹 2-4. 타입·비트필드

| 번호 | 한국어 설명 |
|------|-------------|
| Rule-6.1 | 비트 필드는 적절한 타입으로만 선언 |
| Rule-6.2 | 단일 비트 네임드 비트 필드는 signed 타입 금지 |
| Rule-6.3 | 비트 필드를 union 멤버로 선언 금지 |

## 🔹 2-5. 리터럴·상수

| 번호 | 한국어 설명 |
|------|-------------|
| Rule-7.1 | 8진 상수 사용 금지 |
| Rule-7.2 | unsigned 정수 상수에 `u` 또는 `U` 접미사 필수 |
| Rule-7.3 | 리터럴 접미사에 소문자 `l` 사용 금지 (대문자 `L` 사용) |
| Rule-7.4 | 문자열 리터럴은 `pointer to const-qualified char` 객체에만 대입 |
| Rule-7.6 | 최소폭 정수 상수 매크로의 소형 정수 변종 사용 금지 |

## 🔹 2-6. 선언·정의

| 번호 | 한국어 설명 |
|------|-------------|
| Rule-8.1 | 타입을 명시적으로 지정 |
| Rule-8.2 | 함수 타입은 명명된 파라미터를 가진 프로토타입 형태 |
| Rule-8.3 | 동일 객체·함수의 모든 선언은 같은 이름·타입 한정자 사용 |
| Rule-8.4 | 외부 링키지 객체·함수 정의 시 호환 가능한 선언이 가시적이어야 함 |
| Rule-8.5 | 외부 객체·함수는 단 하나의 파일에서만 선언 |
| Rule-8.6 | 외부 링키지 식별자는 정확히 하나의 외부 정의를 가짐 |
| Rule-8.8 | 내부 링키지 객체·함수의 모든 선언에 static 지정자 사용 |
| Rule-8.10 | 인라인 함수는 static 저장 클래스로 선언 |
| Rule-8.12 | 열거자 리스트 내 암시적 지정 값은 고유해야 함 |
| Rule-8.14 | restrict 타입 한정자 사용 금지 |
| Rule-8.15 | 명시적 정렬 지정 객체의 모든 선언은 같은 정렬 사용 |

## 🔹 2-7. 초기화

| 번호 | 한국어 설명 |
|------|-------------|
| Rule-9.2 | 집합체·union 초기화는 중괄호로 감쌀 것 |
| Rule-9.3 | 배열 부분 초기화 금지 |
| Rule-9.4 | 객체의 한 요소를 두 번 이상 초기화 금지 |
| Rule-9.5 | 지정 초기화자 사용 시 배열 크기를 명시적으로 지정 |
| Rule-9.6 | 연쇄 지정자를 사용하는 초기화자는 지정자 없는 초기화자 포함 금지 |

## 🔹 2-8. 필수 타입 모델 (Essential Type)

| 번호 | 한국어 설명 |
|------|-------------|
| Rule-10.1 | 피연산자는 부적절한 필수 타입 금지 |
| Rule-10.2 | 필수 문자 타입 표현식의 덧셈·뺄셈 부적절 사용 금지 |
| Rule-10.3 | 표현식 값을 좁은/다른 카테고리 필수 타입에 대입 금지 |
| Rule-10.4 | 산술 변환 연산자의 양 피연산자는 동일 필수 타입 카테고리 |
| Rule-10.6 | 복합 표현식 값을 더 넓은 필수 타입 객체에 대입 금지 |
| Rule-10.7 | 복합 표현식이 한 피연산자일 때 다른 피연산자는 더 넓은 타입 금지 |
| Rule-10.8 | 복합 표현식 값을 다른 카테고리/더 넓은 필수 타입으로 캐스팅 금지 |

## 🔹 2-9. 포인터 타입 변환

| 번호 | 한국어 설명 |
|------|-------------|
| Rule-11.1 | 함수 포인터와 다른 타입 간 변환 금지 |
| Rule-11.2 | 불완전 타입 포인터와 다른 타입 간 변환 금지 |
| Rule-11.3 | 객체 타입 포인터 간 서로 다른 타입 캐스팅 금지 |
| Rule-11.6 | void 포인터와 산술 타입 간 캐스팅 금지 |
| Rule-11.7 | 객체 포인터와 비정수 산술 타입 간 캐스팅 금지 |
| Rule-11.8 | 포인터 캐스팅으로 const/volatile 한정자 제거 금지 |
| Rule-11.9 | NULL 매크로만이 정수 널 포인터 상수의 유일한 허용 형태 |
| Rule-11.10 | `_Atomic` 한정자를 불완전 타입 void에 적용 금지 |

## 🔹 2-10. 표현식·부수 효과

| 번호 | 한국어 설명 |
|------|-------------|
| Rule-12.2 | 시프트 연산자 우측 피연산자는 [0, 좌측 비트폭-1] 범위 내 |
| Rule-12.6 | 원자 객체의 구조체·union 멤버 직접 접근 금지 |
| Rule-13.1 | 초기화자 리스트는 지속적 부수 효과 포함 금지 |
| Rule-13.2 | 표현식 값·부수 효과는 평가 순서·스레드 인터리빙과 무관해야 함 |
| Rule-13.5 | 논리 `&&`/`\|\|`의 우측 피연산자는 지속적 부수 효과 포함 금지 |

## 🔹 2-11. 제어문·제어 흐름

| 번호 | 한국어 설명 |
|------|-------------|
| Rule-14.1 | 루프 카운터는 필수 부동소수점 타입 금지 |
| Rule-14.2 | for 루프는 잘 구성된(well-formed) 형태 |
| Rule-14.3 | 제어 표현식은 상수(invariant) 금지 |
| Rule-14.4 | if문·반복문 제어 표현식은 필수 불리언 타입 |
| Rule-15.2 | goto는 같은 함수 내 뒤쪽 레이블로만 점프 |
| Rule-15.3 | goto 참조 레이블은 같은 블록/둘러싸는 블록에 선언 |
| Rule-15.6 | 반복문·선택문 본문은 복합문(compound-statement) |
| Rule-15.7 | 모든 `if ... else if` 구조는 else로 종결 |

## 🔹 2-12. switch 문

| 번호 | 한국어 설명 |
|------|-------------|
| Rule-16.1 | 모든 switch 문은 잘 구성된 형태 |
| Rule-16.2 | switch 레이블은 switch 본문 내 가장 가까운 복합문에서만 사용 |
| Rule-16.3 | 모든 switch 절은 무조건 break로 종결 |
| Rule-16.4 | 모든 switch 문은 default 레이블 필수 |
| Rule-16.5 | default 레이블은 첫 번째 또는 마지막 위치 |
| Rule-16.6 | 모든 switch 문은 최소 2개의 switch 절 필수 |
| Rule-16.7 | switch 표현식은 필수 불리언 타입 금지 |

## 🔹 2-13. 함수

| 번호 | 한국어 설명 |
|------|-------------|
| Rule-17.1 | `<stdarg.h>` 표준 헤더 사용 금지 |
| Rule-17.2 | 재귀 호출 금지 (직접/간접 모두) |
| Rule-17.5 | 배열 타입 파라미터 인자는 적절한 요소 수를 가져야 함 |
| Rule-17.7 | void 아닌 반환 타입 함수의 반환값은 반드시 사용 |
| Rule-17.10 | `_Noreturn` 지정 함수는 void 반환 타입 |
| Rule-17.13 | 함수 타입은 타입 한정(type qualified) 금지 |

## 🔹 2-14. 포인터·배열

| 번호 | 한국어 설명 |
|------|-------------|
| Rule-18.1 | 포인터 산술 결과는 같은 배열 요소를 가리켜야 함 |
| Rule-18.2 | 포인터 간 뺄셈은 같은 배열 요소에만 적용 |
| Rule-18.3 | 관계 연산자(`>`, `>=`, `<`, `<=`)는 같은 객체 포인터에만 적용 |
| Rule-18.6 | 자동/스레드 지역 저장 객체 주소를 소멸 후에도 남는 객체에 복사 금지 |
| Rule-18.7 | 유연 배열 멤버(flexible array member) 선언 금지 |
| Rule-18.8 | 가변 길이 배열(VLA) 사용 금지 |
| Rule-18.9 | 임시 수명 객체의 배열-포인터 변환 금지 |

## 🔹 2-15. 전처리 지시문

| 번호 | 한국어 설명 |
|------|-------------|
| Rule-20.2 | 헤더 파일명에 `'`, `"`, `\`, `/*`, `//` 사용 금지 |
| Rule-20.3 | `#include` 뒤에는 `<filename>` 또는 `"filename"`만 허용 |
| Rule-20.4 | 키워드와 같은 이름의 매크로 정의 금지 |
| Rule-20.6 | 전처리 지시문처럼 보이는 토큰을 매크로 인자 내에 두지 말 것 |
| Rule-20.7 | 매크로 파라미터 확장 결과 표현식은 괄호로 감쌀 것 |
| Rule-20.8 | `#if`/`#elif` 제어 표현식은 0 또는 1로 평가 |
| Rule-20.9 | `#if`/`#elif` 식별자는 평가 전에 `#define`되어야 함 |
| Rule-20.11 | `#` 뒤 매크로 파라미터에 `##` 연산자가 바로 따르지 말 것 |
| Rule-20.12 | `#`/`##` 피연산자 매크로 파라미터는 해당 연산자 피연산자로만 사용 |
| Rule-20.13 | `#`으로 시작하는 라인의 첫 토큰은 유효한 전처리 지시문 |
| Rule-20.14 | `#else`, `#elif`, `#endif`는 관련 `#if`와 같은 파일에 위치 |

## 🔹 2-16. 표준 라이브러리 금지 목록 🚫

| 번호 | 한국어 설명 |
|------|-------------|
| Rule-21.1 | 예약 식별자/매크로에 `#define`, `#undef` 사용 금지 |
| Rule-21.2 | 예약 식별자나 예약 매크로 이름 선언 금지 |
| Rule-21.3 | `<stdlib.h>` 메모리 할당/해제 함수(malloc/free) 사용 금지 |
| Rule-21.4 | `<setjmp.h>` 표준 헤더 사용 금지 |
| Rule-21.5 | `<signal.h>` 표준 헤더 사용 금지 |
| Rule-21.6 | 표준 라이브러리 입출력 함수(printf/scanf 등) 사용 금지 |
| Rule-21.7 | `atof`, `atoi`, `atol`, `atoll` 사용 금지 |
| Rule-21.8 | `abort`, `exit`, `getenv`, `system` 사용 금지 |
| Rule-21.9 | `bsearch`, `qsort` 사용 금지 |
| Rule-21.10 | 표준 라이브러리 시간·날짜 함수 사용 금지 |
| Rule-21.12 | `<fenv.h>` 표준 헤더 사용 금지 |
| Rule-21.14 | `memcmp`로 널 종결 문자열 비교 금지 |
| Rule-21.15 | `memcpy`/`memmove`/`memcmp`의 포인터 인자는 호환 타입 |
| Rule-21.16 | `memcmp` 포인터 인자는 포인터·부호 있음·부호 없음·불리언·enum만 |
| Rule-21.21 | `<stdlib.h>`의 `system` 함수 사용 금지 |
| Rule-21.23 | `<tgmath.h>` 다중 인자 타입 제네릭 매크로 피연산자는 같은 표준 타입 |
| Rule-21.24 | `<stdlib.h>` 난수 생성 함수 사용 금지 |
| Rule-21.25 | 모든 메모리 동기화 연산은 순차 일관성 순서로 실행 |
| Rule-21.26 | `mtx_timedlock()`은 적절한 뮤텍스 타입에서만 호출 |

## 🔹 2-17. 자원 관리 (파일·errno)

| 번호 | 한국어 설명 |
|------|-------------|
| Rule-22.1 | 동적 획득 자원은 반드시 명시적으로 해제 |
| Rule-22.3 | 동일 파일을 다른 스트림에서 동시에 읽기·쓰기 금지 |
| Rule-22.7 | EOF 매크로는 EOF 반환 가능 함수의 수정되지 않은 반환값과만 비교 |
| Rule-22.8 | errno 설정 함수 호출 전에 errno를 0으로 설정 |
| Rule-22.9 | errno 설정 함수 호출 후에 errno를 0과 비교 |
| Rule-22.10 | errno 테스트는 마지막 호출이 errno 설정 함수인 경우에만 |

## 🔹 2-18. 멀티스레드 자원 관리

| 번호 | 한국어 설명 |
|------|-------------|
| Rule-22.11 | 이미 join/detach된 스레드를 다시 join/detach 금지 |
| Rule-22.13 | 스레드 관련 객체는 적절한 저장 기간을 가져야 함 |
| Rule-22.15 | 스레드 동기화 객체는 모든 접근 스레드 종료 전 소멸 금지 |
| Rule-22.16 | 스레드가 잠근 뮤텍스는 같은 스레드가 해제 |
| Rule-22.17 | 잠그지 않은 뮤텍스 해제 또는 `cnd_wait`/`cnd_timedwait` 호출 금지 |
| Rule-22.18 | 비재귀 뮤텍스를 재귀적으로 잠금 금지 |
| Rule-22.19 | 조건 변수는 최대 하나의 뮤텍스 객체와 연결 |

## 🔹 2-19. 제네릭 선택 (_Generic)

| 번호 | 한국어 설명 |
|------|-------------|
| Rule-23.2 | 매크로로부터 확장되지 않은 제네릭 선택의 제어 표현식에 부수 효과 금지 |
| Rule-23.4 | 제네릭 연관은 적절한 타입을 나열 |
| Rule-23.6 | 제네릭 선택 제어 표현식은 표준 타입과 일치하는 필수 타입 |
| Rule-23.8 | default 연관은 제네릭 선택의 첫 번째 또는 마지막 위치 |

---

# 🟡 3부. Advisory (권고 규칙) — 약 39개

> **코드 품질·가독성 향상을 위한 권고.** 

## 🔻 3-1. 코드 정리·가독성

| 번호 | 한국어 설명 |
|------|-------------|
| Rule-1.2 | 언어 확장 사용 자제 |
| Rule-2.3 | 사용되지 않는 타입 선언 자제 |
| Rule-2.4 | 사용되지 않는 태그 선언 자제 |
| Rule-2.5 | 사용되지 않는 매크로 정의 자제 |
| Rule-2.6 | 함수 내 사용되지 않는 레이블 선언 자제 |
| Rule-2.7 | 사용되지 않는 함수 파라미터 자제 |
| Rule-2.8 | 사용되지 않는 객체 정의 자제 |
| Rule-4.2 | 트라이그래프(trigraph) 사용 자제 |

## 🔻 3-2. 식별자·스코프

| 번호 | 한국어 설명 |
|------|-------------|
| Rule-5.9 | 내부 링키지 객체·함수 식별자는 고유한 것이 좋음 |
| Rule-8.7 | 단일 번역 단위에서만 참조되는 객체·함수는 외부 링키지 자제 |
| Rule-8.9 | 단일 함수 내에서만 사용되는 객체는 블록 스코프로 선언 |
| Rule-8.11 | 외부 링키지 배열 선언 시 크기를 명시적으로 지정 |
| Rule-8.13 | 가능한 경우 포인터는 const 한정 타입을 가리킴 |
| Rule-8.16 | 객체 선언에 정렬 0 지정 자제 |
| Rule-8.17 | 객체 선언에 명시적 정렬 지정자는 최대 하나 |

## 🔻 3-3. 타입·포인터

| 번호 | 한국어 설명 |
|------|-------------|
| Rule-10.5 | 표현식 값을 부적절한 필수 타입으로 캐스팅 자제 |
| Rule-11.4 | 객체 포인터와 정수 타입 간 변환 자제 |
| Rule-11.5 | void 포인터에서 객체 포인터로 변환 자제 |

## 🔻 3-4. 표현식·연산자

| 번호 | 한국어 설명 |
|------|-------------|
| Rule-12.1 | 표현식 내 연산자 우선순위를 괄호로 명시 |
| Rule-12.3 | 콤마 연산자 사용 자제 |
| Rule-12.4 | 상수 표현식 평가가 unsigned 정수 래핑을 초래하지 않을 것 |
| Rule-13.3 | `++`/`--` 포함 표현식에 다른 부수 효과 자제 |
| Rule-13.4 | 대입 연산자의 결과 사용 자제 |

## 🔻 3-5. 제어 흐름

| 번호 | 한국어 설명 |
|------|-------------|
| Rule-15.1 | goto 문 사용 자제 |
| Rule-15.4 | 반복문 종료용 break/goto는 하나 이하 |
| Rule-15.5 | 함수는 끝에 단일 출구점을 가짐 |

## 🔻 3-6. 함수 설계

| 번호 | 한국어 설명 |
|------|-------------|
| Rule-17.8 | 함수 파라미터 수정 자제 |
| Rule-17.11 | 반환하지 않는 함수는 `_Noreturn`으로 선언 |
| Rule-17.12 | 함수 식별자는 `&` 앞 또는 파라미터 목록과 함께 사용 |

## 🔻 3-7. 포인터·union

| 번호 | 한국어 설명 |
|------|-------------|
| Rule-18.4 | `+`, `-`, `+=`, `-=`를 포인터 타입에 적용 자제 |
| Rule-18.5 | 포인터 중첩 레벨은 2단계 이하 |
| Rule-19.2 | union 키워드 사용 자제 |

## 🔻 3-8. 전처리

| 번호 | 한국어 설명 |
|------|-------------|
| Rule-20.1 | `#include` 앞에는 전처리 지시문·주석만 위치 |
| Rule-20.5 | `#undef` 사용 자제 |
| Rule-20.10 | `#`, `##` 전처리 연산자 사용 자제 |
| Rule-21.11 | `<tgmath.h>` 표준 헤더 사용 자제 |

## 🔻 3-9. 제네릭 선택

| 번호 | 한국어 설명 |
|------|-------------|
| Rule-23.1 | 제네릭 선택은 매크로에서만 확장 |
| Rule-23.3 | 제네릭 선택은 최소 하나의 비(非)default 연관 포함 |
| Rule-23.5 | 제네릭 선택은 암시적 포인터 타입 변환에 의존 자제 |
| Rule-23.7 | 매크로 확장 제네릭 선택은 인자를 한 번만 평가 |

---